<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <!-- Use the application's global styles -->
    <link rel="stylesheet" href="/Stylesheets/login.css">
</head>
<body>
    <!-- The main content container and the specific form container class are needed for styling -->
    <main class="container form-container">
        <h2>User Login</h2>
        
        <!-- Updated EJS error handling to use the styled class -->
        <% if (typeof error !== 'undefined' && error.length > 0) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        
        <form action="/login" method="POST">
            <!-- Inputs now utilize the full width CSS -->
            <input type="text" name="username" placeholder="Username" required><br>
            <input type="password" name="password" placeholder="Password" required><br>
            
            <!-- Button now uses the application's styled classes -->
            <button type="submit" class="main-button primary">Login</button>
        </form>
        
        <!-- Styled link container -->
        <p class="mt-4">No account? <a href="/register">Register here</a></p>
    </main>
</body>
</html>
